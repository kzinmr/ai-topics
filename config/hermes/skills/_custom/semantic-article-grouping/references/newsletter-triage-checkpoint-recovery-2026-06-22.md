# Newsletter-Triage Checkpoint Recovery (June 22, 2026)

## Scenario
Upstream `newsletter-triage` failed with:
```
"failed to parse JSON response from newsletter-triage output"
```

The error was generated by Hermes's cron response parser, not by the triage agent itself.

## Recovery Steps

1. **Check checkpoint first** at `${HERMES_HOME}/cron/data/newsletter/triage_latest.json`
2. In this session, the file contained valid JSON (127 lines, 13,439 bytes) with:
   - `checkpoint_run_id: "20260622T071007Z"`
   - `summary_ja` with Japanese triage summary
   - 10 decisions (3 takes, 2 references, 5 skips)
   - All `body_excerpt` fields populated
3. **Skip the output file** — no markdown extraction needed
4. **Proceed directly to Post-Recovery Verification**:
   - Verify each `take` independently (check if entity/concept exists, read existing content)
   - Execute verified takes via parallel delegate_task (3 subagents)
   - Update index.md and log.md
   - Archive skip/reference items
   - Commit and push

## Key Differences from Dreaming-Pipeline Recovery

| Dimension | Dreaming | Newsletter |
|-----------|----------|------------|
| Error message | `failed to parse JSON response from dreaming-group output` | `failed to parse JSON response from newsletter-triage output` |
| Checkpoint path | `${HERMES_HOME}/cron/data/dreaming/triage_latest.json` | `${HERMES_HOME}/cron/data/newsletter/triage_latest.json` |
| Output path | `~/.hermes/cron/output/<job-id>/<timestamp>.md` | same pattern |
| Recovery | Same pattern — read checkpoint directly | Same pattern — identical recovery |

## This Session's Verification Results

| Take | Status | Action |
|------|--------|--------|
| Fiona Fung (★★★★★) | No existing page | Created `entities/fiona-fung.md` (50 lines) |
| Codex Record & Replay (★★★★☆) | 316-line page, zero mentions of Record & Replay | Added section + competitive table update |
| Sampling Strategies Batch Invariance (★★★★☆) | 24-line stub (`status:stub`) | Replaced with 41-line full page |

## Pipeline-Specific Notes

- The newsletter-triage agent saved the checkpoint before the response render failed — same behavior as dreaming-group
- The downstream `newsletter-wiki-ingest` consumed `triage_latest.json` and executed enrichment without re-running triage
- Supports that the "save-checkpoint-before-render" pattern is pipeline-agnostic — likely applies to blog-triage too
