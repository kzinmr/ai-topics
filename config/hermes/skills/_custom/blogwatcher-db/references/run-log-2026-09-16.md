# blog_ingest run — 2026-09-16

## Anomaly: `scan.blogs_scanned=0` / `new_articles_per_blog={}` despite real scan

Checkpoint showed:
```
"total_new": 37, "blogs_scanned": 0, "new_articles_per_blog": {}
```
but the blogwatcher DB proved the scan ran fine:
- `MAX(blogs.last_scanned)` = 2026-09-16T10:01:33Z (matches checkpoint scan_done_at)
- DB query `WHERE DATE(discovered_date)='2026-09-16'` returned exactly 37 articles across 16 blogs (AI Engineer 9, Anyscale Blog 7, daringfireball 4, LWN 3, ...).

Conclusion: `total_new` (37) is trustworthy; `blogs_scanned`/`new_articles_per_blog` are a per-blog breakdown that `blog_ingest.py` fails to populate (likely parses blogwatcher-cli stdout text that changed format). Not blocking — downstream triage uses `articles.blog_articles` + `saved_articles`, not the per-blog map.

## Agent-side verification done

- All 17 `saved_articles[*].raw_path` files exist under `wiki/raw/articles/` with valid frontmatter (spot-checked gemini-live, modal product-updates, garymarcus secret-eval).
- Committed 26 untracked raw files (17 from this checkpoint + 9 company-blog captures from sitemap-monitor) as commit 238cadd1, pushed to kzinmr/ai-topics main.
- 3 unsaved: FT paywalled (linked via Daring Fireball), shkspr.mobi RSS Club meta post, LWN paywalled `[$]` article — all acceptable skips.

## AI-relevant highlights for downstream triage

- simonwillison.net: Gemini 3.8 Live speech-to-speech models + browser Web UI
- garymarcus: secret US AI evaluation framework partly revealed
- seangoedecke: Jevons paradox → structured output economics
- pluralistic: AI moratorium argument
- Modal: Sandbox Sidecars, day-zero Kimi K3 / Qwen 3.8 / GLM 5.3 support
- Apple Reference Image (verified photography) via security.apple.com
