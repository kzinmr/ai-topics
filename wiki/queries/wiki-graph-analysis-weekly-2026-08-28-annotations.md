# Weekly Wiki Graph Analysis — 2026-08-28 Annotations

> Manual annotation of `wiki-graph-analysis-weekly-2026-08-28.md` generated by the
> wiki-graph-analysis cron job. Records false positives in the automated metrics
> so future runs are interpreted correctly. See log entry 2026-08-28.

## False positives in automated metrics

### "Not indexed" / "stale index entries" (reported 27 / 7 — real gaps: 2 / 1)

`_weekly_graph_report.py`'s index reconciliation misclassifies two categories:

1. **Directory hub pages** (`concepts/anthropic`, `concepts/claude`, `concepts/gemini`,
   `concepts/gpt`, `concepts/openai`, `concepts/context-engineering`,
   `concepts/ai-benchmarks`, `entities/bill-gates`, `entities/louis-abraham`) —
   the script maps `foo/index.md` to slug `foo` but never tries `foo/_index.md`,
   and the hub pages are intentionally indexed as `[[entities/_index]]`-style
   child entries, not as bare `[[concepts/anthropic]]`. → Not real gaps.
2. `concepts/gpt/_archive/*` — archived pages, correctly excluded from index.

Genuine index gaps found by this week's manual pass:
- `concepts/ai-employment-displacement` — 131 lines, on disk, missing from index.md → **add it**
- `queries/wiki-graph-analysis-weekly-2026-08-21` — index entry left stale when the
  report generator deleted last week's report file (fixed 2026-08-28).

## Duplicate groups — adjudicated verdicts (16 reported → 5 real, 6 redirects, 5 false positives)

| Group | Verdict | Action |
|-------|---------|--------|
| entities/anthropic-style `index` (entities/_index, concepts/_index) | False positive — two distinct navigation hubs | none |
| entities/cline vs concepts/cline | Not a dup — entity (product org) vs concept (autonomous coding agent paradigm) | none |
| entities/qwen vs concepts/qwen | Borderline — entity (model family/org, 2026-08-13) vs concept (older model-family analysis, 2026-06-09) | consider merging concept content into entity page |
| entities/deliberate-coder vs entities/deliberatecoder | NOT a dup — Ben Ilegbodu vs Steve Shogren (different people!) | rename deliberate-coder → `ben-ilegbodu` to disambiguate |
| entities/eugene-yan vs entities/eugeneyan | REAL dup — same person, both rich (352/220 lines, diverged narratives) | merge → keep `eugeneyan` handle slug, fold in eugene-yan + its `--core-ideas/--key-quotes/--timeline` subpages' links |
| entities/giles-thomas vs entities/gilesthomas | REAL dup — same person (734-line total), different content angles | merge into one page |
| entities/lilian-weng vs entities/lilianweng | REAL dup — same person, CONFLICTING facts: weng page says "VP Research at OpenAI, Preparedness lead"; lilian-weng says co-founded Thinking Machines Lab. Needs dated reconciliation, not silent overwrite | merge + mark contested until resolved |
| entities/martin-fowler vs entities/martinfowler | Redirect pattern — intentional alias page | none (dedup in future report) |
| entities/samuel-colvin vs entities/samuelcolvin | Redirect pattern | none |
| concepts/agent-harnesses vs comparisons/agent-harnesses | Not a dup — concept (design patterns) vs comparison (9-harness table) | none |
| concepts/alpha-proof-nexus vs concepts/alphaproof-nexus | REAL dup — same title, both small; merge into one | merge |
| concepts/deer-flow vs concepts/deerflow | REAL dup — same project, older/smaller `deerflow` stub | merge into deer-flow |
| concepts/dspy-rlm vs concepts/dspyrlm | Redirect pattern (dspyrlm = stub → dspy-rlm) | none |
| concepts/evals-skills vs comparisons/evals-skills | Redirect pattern (25-line April stub with `[[entities/_index]]` footer) | none |
| concepts/llm-integration-patterns vs comparisons/llm-integration-patterns | Redirect pattern (same as above) | none |
| concepts/open-claw-ecosystem vs concepts/openclaw-ecosystem | Redirect pattern (same stub family) | none |

Note: the ~25-line stub pages (open-claw-ecosystem, evals-skills,
llm-integration-patterns) share a signature: `created: 2026-04-25`, empty
`sources:`, trailing `[[entities/_index]]` link — they came from one April bulk
generation. Recommend converting them to proper `redirect:` pages like
`entities/martin-fowler.md` so the graph treats them as aliases, not pages.

## Generator improvements for next run

1. Index reconciliation: resolve `foo/_index.md` → slug `foo`; skip `_archive/`
   paths; treat hub pages indexed via their child entries as covered.
2. Duplicate detector: skip pages whose frontmatter has `redirect:` (they are
   intentional aliases) — this removes 6 of 16 reported groups.
3. Report writer: when deleting last week's report, also replace its index.md
   entry (or keep reports with dated filenames and index only the latest).
4. Consider excluding `*_index` and `index` slugs from the dup-name matcher.
