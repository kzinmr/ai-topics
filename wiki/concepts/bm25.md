---
title: BM25
created: 2026-05-18
updated: 2026-05-28
type: concept
tags:
  - bm25
  - search
  - lexical-search
  - agentic-engineering
  - rag
  - optimization
sources:
  - raw/articles/2026-05-15_recsys_bm25-agentic-deep-research.md
  - raw/articles/2026-05-17_softwaredoug-com_autoresearching-better-msmarco-bm25.md
  - raw/articles/2025-09-18_softwaredoug_bm25f-from-scratch.md
  - raw/articles/2026-05-13_softwaredoug_lexical-search-bm25.md
---

# BM25

BM25 (Best Matching 25) is a classical **lexical retrieval** algorithm that ranks documents by term frequency and inverse document frequency. For decades it was the workhorse of search engines (Elasticsearch, Lucene) but was largely considered "obsolete" for modern AI applications — until PI-SERINI (May 2026) demonstrated that BM25 + a capable LLM in an agentic loop can **beat dense neural retrievers** on deep research benchmarks while being 3.3x–10x cheaper.

> **Key insight (Hsu et al., 2026):** "BM25 is sufficient for deep research when paired with capable LLMs in an agentic loop, challenging the assumption that dense retrievers are necessary."

## Algorithm

BM25 scores a document $d$ against a query $q$ as:

$$\text{BM25}(q, d) = \sum_{t \in q} \text{IDF}(t) \cdot \frac{f(t,d) \cdot (k_1 + 1)}{f(t,d) + k_1 \cdot (1 - b + b \cdot \frac{|d|}{\text{avgdl}})}$$

Where:
- $f(t,d)$ = term frequency of $t$ in $d$
- $|d|$ = document length
- $\text{avgdl}$ = average document length in corpus
- $k_1$ = term frequency saturation parameter (default: 0.9 in Anserini/Lucene)
- $b$ = document length normalization (default: 0.4 in Anserini/Lucene)
- $\text{IDF}(t)$ = inverse document frequency

## BM25F: Multi-Field BM25

BM25F extends BM25 to documents split across **multiple fields** (e.g., `title`, `body`, `description`), where each field has its own statistics (average length, DF, TF). Naive BM25 across fields introduces two critical problems, each addressed by BM25F's two-step correction.

> "BM25F from scratch" — Doug Turnbull (Sep 2025)

### BM25 Building Blocks (Deconstructed)

BM25's scoring rests on three statistical primitives, each refined by decades of IR research:

| Component | Raw Stat | BM25 Refinement | Param |
|-----------|----------|-----------------|-------|
| Term Frequency (TF) | Raw count | **Saturation**: $tf/(tf+k_1)$ — plateaus after ~5-10 occurrences | $k_1$ |
| Document Frequency (DF) | Raw rarity | **Logarithmic IDF**: $\log(1+(N-df)/(df+0.5))$ — specificity decays non-linearly | — |
| Field Length | Raw word count | **Length scaling**: $tf/(1-b+b\cdot dl/avgdl)$ — short fields weighted higher | $b$ |

The full BM25 TF term that integrates all three:

$$\text{bm25\_tf} = \frac{tf}{tf + k_1 \cdot (1 - b + b \cdot \frac{dl}{avgdl})}$$

> **Key mental model**: "BM25 ≈ Relevance Given Match" — assuming a term match occurred, how much is this passage *about* that matched term?

### Problem 1: Field-Specific IDF Distortion

When each field maintains its own DF statistics, a term that is **rare in one field** but globally common gets an erroneously high IDF:

**Example**: Searching `javascript book`. `book` is rare in the `title` field but ubiquitous in `body`. The title's IDF for `book` explodes, ranking "The big book on squirrels" above actual JavaScript books.

```
Naive sum:  score = TF(title,book) × IDF(title,book)
                   + TF(body,book)  × IDF(body,book)
             → "book" in title dominates, no javascript required
```

### Problem 2: Independent TF Saturation (Double Counting)

Summing per-field BM25 scores allows **each field's TF to climb the saturation curve independently**. A title match of tf=1 and a body match of tf=100 are both scored from the "early" steep part of their respective curves:

| Match | Raw TF | Saturation Stage | Effect |
|-------|--------|------------------|--------|
| Title | 1 | **Steep** (early) | Overweighted |
| Body | 100 | Flat (late) | Underweighted |
| Sum | 101 | **Double counts both early slopes** | Inflated score |

What we actually want: Title+Body combined as a single tf=101 (after length normalization), saturated **once**.

But there's a deeper issue: tf=1 in a 5-word title and tf=100 in a 10,000-word body are **incommensurable** — we must length-normalize each before summing.

### BM25F: The Two-Step Correction

**Step 1 — Blend Document Frequencies**: Use the **maximum** DF across all fields to approximate corpus-wide specificity:

```python
combined_doc_freq = max(DF('title', term), DF('body', term))
blended_idf = compute_idf(corpus_len, combined_doc_freq)
```

This is what Elasticsearch's `cross_fields` query mode does — an "80% solution" that fixes the IDF distortion but not the TF double-counting.

**Step 2 — Length-Normalize, Then Saturate Together**:

```python
# Per-field length normalization FIRST
bm25f_tf = bm25_tf(
    scaled_tf(TF('title', term), title_len, avg_title_len) +
    scaled_tf(TF('body', term),  body_len,  avg_body_len)
)
```

The order is critical: `scaled_tf` per field → **sum** → `bm25_tf` (saturate once). This ensures:
- Title tf=1 (short field) gets amplified by length normalization
- Body tf=100 (long field) gets dampened  
- The sum goes through saturation **as a single value**

**Final BM25F score**:

```python
bm25f = blended_idf * bm25f_tf
```

### Design Principle

BM25F's core insight: you cannot sum raw term frequencies across fields — it's "apples to oranges." Each field's TF must first be converted to an **effective, length-normalized TF**, making them comparable before summation and saturation.

An alternative view from Doug Turnbull's slide deck (2026) simplifies to: saturate combined TF across all fields together, then take the max IDF. Both formulations converge on the same principle — **unified blending before scoring**.

## BM25 in the Agentic Era

### PI-SERINI: BM25 + LLM Agent Loop

The PI-SERINI paper (Hsu et al., May 2026) showed that BM25 lexical retrieval, when embedded in an agentic search loop with a capable LLM, achieves **state-of-the-art deep research performance**:

**Architecture:**
- Three explicit tools: `search`, `read_search_results`, `read_document`
- LLM caches rankings of up to **1000 documents** and browses them selectively
- Documents are NOT dumped inline — the agent actively chooses what to read
- **Wall-clock budget** (300 seconds) replaces fixed iteration cap, with a submission steer injected at 70% of timeout

**BM25 Tuning for Agentic Use:**
- Standard Anserini defaults ($k_1=0.9$, $b=0.4$) are tuned for short web documents
- For BrowseComp-Plus (long, noisy documents): **$k_1=25$, $b=1$** — dramatic reparameterization
- Retrieval depth increased to **1000** (vs typical top-10/20)

**Results:**
- gpt-5.5 + PI-SERINI: **83.1% answer accuracy**, **94.7% evidence recall** on BrowseComp-Plus
- Beats all released dense-retriever agents
- **3.3x–10x cost reduction** via prefix caching (82–90% of tokens served from cache)
- The agentic loop itself is cache-friendly because the system prompt and tool definitions are static

**Failure Mode: "Premature Branch Commitment"**
- claude-opus-4.7 keeps probing weak hypotheses; gpt-5.5 backtracks to original clues
- This explains part of the gap between otherwise comparable frontier models
- Implication: the LLM's **search strategy** matters as much as retrieval quality

## BM25 vs Dense Retrieval

| Dimension | BM25 (Lexical) | Dense Retrieval (Embeddings) |
|---|---|---|
| **Mechanism** | Exact term matching + TF-IDF statistics | Semantic similarity in embedding space |
| **Cost** | Near-zero (no GPU, no embeddings) | Embedding computation for queries + documents |
| **Cache friendliness** | Stateless, no model inference needed | Embeddings can be precomputed but still require storage |
| **Strengths** | Exact keyword matches, rare terms, transparency | Semantic understanding, synonym handling, multilingual |
| **Weaknesses** | Vocabulary mismatch, no semantic understanding | Embedding model quality dependent, compute cost |
| **Agentic synergy** | Cache-friendly (82-90% prefix cache hit in PI-SERINI) | Higher per-query latency, less cache benefit |

## Why This Matters

1. **Cost efficiency**: BM25 is essentially free compared to embedding-based retrieval. For agentic loops that make dozens of search calls, this compounds.
2. **Prefix caching**: The static system prompt + BM25 as a deterministic retriever means most tokens are cached — a property dense retrievers don't share.
3. **Transparency**: BM25 rankings are interpretable (you can see which terms matched). This helps LLMs reason about search results.
4. **Re-evaluates assumptions**: The field has been moving toward denser, larger embedding models. PI-SERINI suggests the bottleneck is the **agent's reasoning about search results**, not retrieval quality.

## Related Concepts

- [[concepts/autoresearch-bm25-msmarco]] — Doug Turnbull's case study: agent-driven BM25 tuning on MSMarco
- [[concepts/lexical-search]] — Token-based matching pipeline that BM25 scores
- [[concepts/dense-retrieval]] — Neural/embedding-based search
- [[concepts/agentic-search]] — LLM-driven iterative search loops
- [[rag]] — Retrieval-Augmented Generation
- [[entities/embeddings]] — Dense vector representations underlying neural retrieval
- [[entities/pi-serini]] — Reference implementation of BM25 + agentic search

## References

- Hsu et al., "Pareto-Efficient Deep Research with BM25 and Capable LLMs" (arXiv:2605.10848, May 2026)
- [PI-SERINI GitHub](https://github.com/justram/pi-serini)
- [RecSys Newsletter Vol.156](https://recsys.substack.com/p/is-bm25-enough-for-agentic-deep-research)
