---
title: "Open models lag state-of-the-art closed models by 4 months"
source_url: "https://epoch.ai/data-insights/open-closed-eci-gap"
publication: "Epoch AI"
date: "2026-05-28"
author: "Epoch AI Research Team"
tags: ["benchmark", "model", "open-source", "evaluation"]
---

# Open models lag state-of-the-art closed models by 4 months

Since January 2026, the most capable open-weight models have lagged frontier closed models by an average of four months in the Epoch Capabilities Index (ECI), Epoch AI's aggregate measure of model capability. The average ECI gap was 8 points, similar to the gap between GPT-5 and GPT-5.5.

This gap between open and closed models is slightly larger than the one identified in Epoch AI's October 2025 Data Insight, which found that open models lagged by an average of three months between January 2023 and October 2025.

## Methodology

Epoch AI calculates the average amount of time it takes for the best open-weight models to catch up with state-of-the-art performance according to the Epoch Capability Index (ECI). ECI is a composite measure that captures performance across many benchmarks.

To calculate the average time gap, they proceed day by day over the analysis window (January 1, 2026 through May 28, 2026). For each day, they identify the open-weight model with the highest ECI score available by that date. They then compare that model to the historical state-of-the-art ECI frontier: what is the most recent date on which the SOTA model was not significantly better than this open-weight model? The time gap for that day is the number of days elapsed since that date.

Because ECI scores are estimated with uncertainty, they use bootstrap samples to make this comparison. Bootstrap samples are generated by resampling the full set of benchmark scores with replacement and refitting the ECI model on each resampled dataset. For each bootstrap sample, they compare the open-weight model's bootstrapped ECI estimate to the bootstrapped ECI estimate of each historical SOTA model, preserving the pairing between bootstrap samples across models. They treat the open-weight model as having plausibly caught up to a previous SOTA model if it outperforms that SOTA model in at least 5% of paired bootstrap samples (i.e., the previous SOTA model is not significantly better than the open-weight model at the 5% level). They then use the most recent historical SOTA date satisfying this criterion to compute the time gap.

**Key findings:**
- Average time gap: **four months** (would grow to six months if requiring the open-weight model's point estimate for ECI to be strictly higher than the closed-weight model it is catching up to)
- Average ECI gap: **8 points** (90% CI: 7 to 11 units)

## Limitations

Epoch AI only includes models with enough public benchmark coverage to assign an ECI. Leading closed labs do not always release their most capable models — for safety, commercial, or competitive reasons — so this analysis likely understates the true open-vs-closed gap whenever closed labs are sitting on more capable models that have not yet been published.

## Related Insights

- **October 2025 Data Insight**: "Open-weight models lag state-of-the-art by around 3 months on average" — covering January 2023 through October 2025
